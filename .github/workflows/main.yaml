name: Deploy Dev Preview

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  Lint-and-Format:
    name: Lint with Ruff and Format with Ruff
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: ".python-version"

      - name: Install dependencies
        run: uv sync --all-extras --dev --frozen

      - name: Lint
        run: uv run ruff check .

      - name: Check Formatting
        run: uv run ruff format --check .

  Test:
    name: Test with Pytest
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: ".python-version"

      - name: Install dependencies
        run: uv sync --all-extras --dev --frozen

      - name: Run Tests
        run: uv run pytest

  UI-Checks:
    name: Check the admin console (Prettier, oxlint, tsc)
    runs-on: ubuntu-latest
    # Deliberately not in Build-Push-Image's `needs`: `ui/` is excluded from the
    # backend image (see .dockerignore), so console code cannot affect the
    # artifact this workflow ships. Require it through branch protection to gate
    # merges; leaving it out of `needs` keeps a console lint error from blocking
    # a backend deploy.
    defaults:
      run:
        working-directory: ui
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: ui/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check Formatting
        run: pnpm run format:check

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

  Build-Push-Image:
    name: Build Docker image
    runs-on: ubuntu-latest
    needs: [Lint-and-Format, Test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: ".python-version"

      - name: Install dependencies
        run: uv sync --all-extras --dev --frozen

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_REGISTRY_USERNAME }}
          password: ${{ secrets.DOCKER_REGISTRY_TOKEN }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v7
        with:
          cache-from: type=gha
          cache-to: type=gha,mode=max
          push: true
          tags: |
            nycusdc/sciedu-llm:dev
            nycusdc/sciedu-llm:${{ github.sha }}
          context: .

  Deploy:
    name: Deploy
    needs: Build-Push-Image
    runs-on: ubuntu-latest
    steps:
      - name: Trigger n8n Snapshot Webhook
        run: |
          curl -X POST --location --fail-with-body 'https://webhook.sdc.nycu.club/webhook/deploy' \
            --header 'Content-Type: application/json' \
            --header 'x-deploy-token: ${{ secrets.N8N_DEPLOY_TOKEN }}' \
            --data-raw '{
            "source": {
              "title": "SciEdu",
              "repo": "${{ github.repository }}",
              "branch": "${{ github.head_ref || github.ref_name }}",
              "commit": "${{ github.sha }}"
            },
            "method": "deploy",
            "metadata": {
              "environment": "dev",
              "component": "llm"
            },
            "setup": {
              "inject_secret": {
                "enable": true,
                "project": "sciedu",
                "environment": "dev",
                "secrets": [
                  {
                    "path": "/LLM",
                    "secret_name": "NCHC_API_KEY",
                    "env_name": "OPENAI_API_KEY"
                  },
                  {
                    "path": "/LLM",
                    "secret_name": "LANGFUSE_SECRET_KEY",
                    "env_name": "LANGFUSE_SECRET_KEY"
                  },
                  {
                    "path": "/LLM",
                    "secret_name": "LANGFUSE_PUBLIC_KEY",
                    "env_name": "LANGFUSE_PUBLIC_KEY"
                  }
                ]
              }
            },
            "post": {
              "notify_discord": {
                "enable": true,
                "channel": "sciedu-activity"
              },
              "setup_domain": {
                "enable": true,
                "title": "Endpoint",
                "name": "llm.dev.sciedu.sdc.nycu.club",
                "value": "default-eng-deploy:internal"
              }
            }
          }'
