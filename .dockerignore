# Backend image build context (root Dockerfile). The image ships the Python
# service only — the admin console has its own image (ui/Dockerfile).
.git
.venv
.env
__pycache__
**/__pycache__
.pytest_cache
.ruff_cache

# Not part of the runtime service
ui
ui-mockup
*.zip
tests
docs
data
scripts
.deploy
.github
.claude
